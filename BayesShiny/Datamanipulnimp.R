library(qpcR)

# Run the Bay

padding  <- function(a,b) {
  zz <-rep(NA,length(X)-length(Y))
  YY  <- c(Y,zz)
  out <- data.frame(cbind(a,YY))
}

x = data.frame.na(y1,y2) 
library(xlsx)
write.xlsx(x, "/home/anupam/Desktop/mydata.xlsx")